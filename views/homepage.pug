doctype html
html 
    head 
        title= `Homepage`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Games
        a Add to this table
        a(href='/update-cobj') Sign out

        .cards 
            each game in data
                .card 
                    h2 #{game.properties.game_name}  #{game.properties.genre}
                    p #{game.properties.main_character}